//thjs file contains the JSK for the header component
// of the app

//import libraries
import React from 'react';
import { Text } from 'react-native';

//define component
//fn name = containing file name (rule of thumb)
const Header = () => {
    return <Text> THIS IS THE HEADER </Text>;
};

//expose component to app (NOT RENDER)
export default Header;
//makes Header component available to other files
